---
import BaseLayout from '../layouts/BaseLayout.astro';

const heroStats = [
  { value: '+68%', label: 'モバイル経由の予約率', description: 'フォーム導線の統一で離脱率を半減' },
  { value: '200%', label: 'Web新規予約', description: '初回特典をスマホで完結させる導線に最適化' },
  { value: '+72%', label: '平均セッション時間', description: 'ストーリーテリングで体験価値を可視化' }
];

const beforeAfter = [
  {
    title: 'Before',
    points: [
      '情報が古く更新も止まったまま',
      'スマホ表示時の文字サイズが小さく離脱率が高い',
      '予約方法が電話のみで新規のお客様が迷子に'
    ]
  },
  {
    title: 'After',
    points: [
      '施術メニューと効果をモバイルカードで再編成',
      '予約導線を一本化し、CTAの文言をシナリオ別に最適化',
      '世界観を守りながらも200%の新規予約を実現'
    ]
  }
];

const dailyScenarios = [
  {
    time: 'Morning',
    label: '朝 — 目覚めを整える',
    menu: 'ハーバル・ディープブレス 60min',
    detail: '体の巡りを整えるショートコース。出勤前の深呼吸習慣に。',
    accent: '初回 5,500 THB → 4,400 THB'
  },
  {
    time: 'Day',
    label: '昼 — 仕事の合間',
    menu: 'タイ古式ストレッチ 90min',
    detail: 'ガイド付きのストレッチと指圧で、巡りと姿勢を同時に整える。',
    accent: 'リピーター人気 No.1'
  },
  {
    time: 'Night',
    label: '夜 — 深い休息へ',
    menu: 'ロイヤルバームセラピー 120min',
    detail: '温熱ハーブボールとアロマで、自律神経を優しくほどく贅沢ケア。',
    accent: 'ペア来店で10%オフ'
  }
];

const courses = [
  {
    name: 'Herbal Awakening',
    duration: '60分',
    price: '5,500 THB',
    ideal: '初回 / クイックリフレッシュ',
    effects: ['ハーブボール温熱', '首肩のディープリリース', 'ハーブティーのテイスティング'],
    result: '短時間で心拍と呼吸を整え、集中力を取り戻す'
  },
  {
    name: 'Thai Stretch Flow',
    duration: '90分',
    price: '7,800 THB',
    ideal: 'リピーター / 全身の巡り',
    effects: ['タイ古式ストレッチ', '骨盤調整マニュアルケア', '足浴＋リフレクソロジー'],
    result: '可動域を広げ、深い睡眠に繋がる循環をサポート'
  },
  {
    name: 'Royal Botanica',
    duration: '120分',
    price: '10,500 THB',
    ideal: 'ギフト / ご褒美ケア',
    effects: ['シグネチャーアロマオイル', 'バンコク寺院モチーフのマインドフルネス', 'アフターカウンセリング'],
    result: '五感すべてを満たし、翌日まで続く軽さと艶を実感'
  }
];

const experienceSteps = [
  {
    title: '01. 体験リサーチ',
    description: '五感のチェックシートで香り・温度・音の好みを共有。心と身体の現在地を一緒に確認します。',
    tone: 'Dawn',
    detail: '柔らかな朝霧の照明と、ベルガモットの香りで呼吸を整える導入。'
  },
  {
    title: '02. コンセプトメイキング',
    description: '「深呼吸がしたくなる余白」を軸に、カスタムメニューを設計します。',
    tone: 'Noon',
    detail: '寺院のモチーフから着想したリズムで、心身の緊張を緩める準備タイム。'
  },
  {
    title: '03. デザイン・実装',
    description: '施術フローとFAQをスマホ中心に構築。フォームの入力進行度を可視化し不安をゼロへ。',
    tone: 'Dusk',
    detail: '葉脈が揺らめくような照明演出で、施術中の没入感を高めます。'
  },
  {
    title: '04. 運用サポート',
    description: 'キャンペーン更新ガイド、SNS連携、レビュー返信テンプレートまでをセットでご提供。',
    tone: 'Night',
    detail: '施術後のアフターケアを自然と続けられる、音声ガイドを含むケアノート。'
  }
];

const voices = [
  {
    name: '初回のお客様',
    role: 'IT企業 / 30代',
    message: '「予約までの流れが手のひらの中で完結。香りや音まで想像できて、初めてでも安心でした。」'
  },
  {
    name: 'リピーター',
    role: '旅行ライター / 40代',
    message: '「時間帯ごとのおすすめがわかりやすく、仕事の合間にどのコースが良いかすぐ選べます。」'
  },
  {
    name: 'ギフト利用',
    role: '外資系エグゼクティブ / 50代',
    message: '「ギフト用の文言とカラーが用意されていて、贈る相手にも世界観が伝わるのが嬉しいです。」'
  }
];

const checklist = [
  '当日は10分前のご来店を推奨しております。',
  'お着替え・アメニティを完備しておりますので手ぶらでお越しください。',
  '体調やアレルギーについて事前にフォームへご記入ください。'
];
---

<BaseLayout pageClass="home">
  <section class="hero shell" id="top">
    <div class="hero-pattern" aria-hidden="true"></div>
    <div class="hero-content">
      <p class="tag">Thai Massage Salon</p>
      <h1>深い呼吸と余白の時間を、Webでも。</h1>
      <p class="hero-sub">
        森の深緑とタイの伝統モチーフを纏ったSiam Healingのデジタル体験。店舗と変わらぬ安心感で、初めての方も迷わず予約へたどり着けます。
      </p>
      <div class="hero-actions">
        <a class="button" href="#reservation">今すぐ予約</a>
        <a class="button secondary" href="#contact">ご相談はこちら</a>
      </div>
      <div class="hero-metrics">
        {heroStats.map((stat) => (
          <article class="metric-card card" data-label={stat.label}>
            <p class="metric-value">{stat.value}</p>
            <p class="metric-label">{stat.label}</p>
            <p class="metric-description">{stat.description}</p>
          </article>
        ))}
      </div>
    </div>
    <div class="hero-visual">
      <div class="visual-frame">
        <span class="visual-caption">森の静けさを宿した施術ルーム</span>
        <div class="visual-gradient"></div>
      </div>
      <div class="visual-thumbs">
        <div>
          <p>アロマオイルとハーブのディテールを写真＋テクスチャで表現</p>
        </div>
        <div>
          <p>葉脈が揺れるインタラクションを今後追加予定</p>
        </div>
      </div>
    </div>
  </section>

  <section class="shell section-gap" id="case">
    <header class="section-heading">
      <span>Before → After</span>
      <h2>癒やしの価値を損なわずに、モバイル中心へ再構築</h2>
    </header>
    <div class="grid">
      {beforeAfter.map((item) => (
        <article class="card before-after">
          <h3>{item.title}</h3>
          <ul>
            {item.points.map((point) => <li>{point}</li>)}
          </ul>
        </article>
      ))}
    </div>
  </section>

  <section class="concept shell section-gap" id="concept">
    <header class="section-heading">
      <span>Design Concept</span>
      <h2>「深呼吸がしたくなる余白」をデザインに。</h2>
    </header>
    <div class="concept-grid">
      <article class="card concept-card">
        <h3>Visual Language</h3>
        <p>
          深いグリーンとサンドベージュを基調に、ロゴカラーのゴールドでアクセント。柔らかな陰影でアナログな温かさを描き出し、
          画面越しでも身体がほぐれる安心感を演出しました。
        </p>
        <div class="palette">
          <span style="--swatch:#064E3B">#064E3B</span>
          <span style="--swatch:#0f766e">#0F766E</span>
          <span style="--swatch:#d6c7a1">#D6C7A1</span>
          <span style="--swatch:#caa969">#CAA969</span>
        </div>
      </article>
      <article class="card concept-card">
        <h3>Content Architecture</h3>
        <p>
          「お悩み → おすすめコース → 予約」の順で導くストーリー設計。施術時間・価格・効果を比較できるカードUIで不安を解消し、
          モバイルでも読みやすい余白とタイプスケールを追求しました。
        </p>
        <ul class="concept-list">
          <li>スクロールに合わせて背景が透過 → 深緑へ変化するナビゲーション</li>
          <li>クーポン・回数券はタグで整理し、ユーザー属性で並び替え</li>
          <li>多言語切り替えでも崩れない、アイコンとラベルのモジュール化</li>
        </ul>
      </article>
      <article class="card concept-card">
        <h3>Interaction</h3>
        <p>
          葉脈がふわりと揺れるスクロールアニメーションを設計（今後実装予定）。フォームでは入力進行度を可視化し、安心感を補強します。
        </p>
        <div class="interaction-note">
          <p>フォーム導線は「今すぐ予約」と「ご相談」のCTAを2種類用意し、目的別に自然誘導。</p>
          <p>初回 / リピーター / ギフトのシナリオ別CTAで迷いをゼロに。</p>
        </div>
      </article>
    </div>
  </section>

  <section class="shell section-gap" id="experience">
    <header class="section-heading">
      <span>Experience Flow</span>
      <h2>施術体験を自然の移ろいになぞらえて構成。</h2>
    </header>
    <div class="grid experience-grid">
      {experienceSteps.map((step) => (
        <article class="card experience-card">
          <p class="eyebrow">{step.tone}</p>
          <h3>{step.title}</h3>
          <p>{step.description}</p>
          <p class="card-detail">{step.detail}</p>
        </article>
      ))}
    </div>
  </section>

  <section class="dynamic shell section-gap" id="daily">
    <header class="section-heading">
      <span>Dynamic Menu</span>
      <h2>朝・昼・夜でおすすめメニューが切り替わる設計。</h2>
    </header>
    <div class="dynamic-grid">
      {dailyScenarios.map((scenario) => (
        <article class="card dynamic-card" data-time={scenario.time}>
          <p class="dynamic-time">{scenario.label}</p>
          <h3>{scenario.menu}</h3>
          <p>{scenario.detail}</p>
          <span class="dynamic-accent">{scenario.accent}</span>
        </article>
      ))}
    </div>
  </section>

  <section class="shell section-gap" id="courses">
    <header class="section-heading">
      <span>Courses</span>
      <h2>施術時間・価格・効果を比較できるカードUI。</h2>
    </header>
    <div class="grid course-grid">
      {courses.map((course) => (
        <article class="card course-card">
          <div class="course-header">
            <h3>{course.name}</h3>
            <p class="course-meta">{course.duration} / {course.price}</p>
            <p class="course-ideal">{course.ideal}</p>
          </div>
          <ul>
            {course.effects.map((effect) => <li>{effect}</li>)}
          </ul>
          <p class="course-result">{course.result}</p>
          <a class="button secondary" href="#reservation">このコースで予約</a>
        </article>
      ))}
    </div>
  </section>

  <section class="shell section-gap results" id="results">
    <header class="section-heading">
      <span>Results</span>
      <h2>予約導線最適化の成果。</h2>
    </header>
    <div class="grid results-grid">
      <article class="card result-card">
        <h3>モバイル経由の予約率 +68%</h3>
        <p>フォームステップの可視化と入力補助で、安心感を高めながらコンバージョン向上。</p>
      </article>
      <article class="card result-card">
        <h3>Web新規予約 200%</h3>
        <p>初回CTAをゴールド、リピーターをグリーン、ギフトをサンド色で色分けし迷いを削減。</p>
      </article>
      <article class="card result-card">
        <h3>口コミ投稿 1.8倍</h3>
        <p>施術後のケアノートからレビュー投稿導線へ自然に遷移し、体験の温度感をそのまま維持。</p>
      </article>
    </div>
  </section>

  <section class="shell section-gap voice" id="voice">
    <header class="section-heading">
      <span>Voice</span>
      <h2>お客様の声はボイスノート風カードで温度感を再現。</h2>
    </header>
    <div class="grid voice-grid">
      {voices.map((voice) => (
        <article class="card voice-card">
          <p class="voice-message">{voice.message}</p>
          <p class="voice-name">{voice.name}</p>
          <p class="voice-role">{voice.role}</p>
        </article>
      ))}
    </div>
  </section>

  <section class="shell section-gap reservation" id="reservation">
    <header class="section-heading">
      <span>Reservation</span>
      <h2>リラックスした気持ちのまま、予約完了まで。</h2>
    </header>
    <div class="reservation-wrapper">
      <div class="reservation-progress">
        <div class="progress-track" aria-hidden="true"></div>
        <ol>
          <li>
            <span>Step 01</span>
            <strong>コースを選ぶ</strong>
            <p>朝・昼・夜のおすすめから最適なコースを確認。</p>
          </li>
          <li>
            <span>Step 02</span>
            <strong>コンディション記入</strong>
            <p>アレルギーや重点ケア部位をフォームで共有。</p>
          </li>
          <li>
            <span>Step 03</span>
            <strong>決済と確認</strong>
            <p>ウォレットアプリ決済に対応。確認メールで当日の流れをご案内。</p>
          </li>
        </ol>
      </div>
      <form class="reservation-form card" aria-labelledby="reservation-title">
        <div class="cta-switcher" role="radiogroup" aria-label="目的に合わせてお選びください">
          <input type="radio" name="scenario" id="cta-first" defaultChecked />
          <label for="cta-first">初回限定</label>
          <input type="radio" name="scenario" id="cta-repeat" />
          <label for="cta-repeat">リピーター</label>
          <input type="radio" name="scenario" id="cta-gift" />
          <label for="cta-gift">ギフト</label>
        </div>
        <p class="reservation-intro">
          直近7日以内の空き枠をご案内します。チャットコンシェルジュが1時間以内に折り返しご連絡します。
        </p>
        <label>
          ご希望日時
          <input type="datetime-local" required />
        </label>
        <label>
          参加人数
          <select required>
            <option value="1">1名</option>
            <option value="2">2名</option>
            <option value="3">3名</option>
          </select>
        </label>
        <label>
          メールアドレス
          <input type="email" placeholder="you@example.com" required />
        </label>
        <label>
          ご相談事項
          <textarea rows="3" placeholder="重点的にほぐしたい部位や、香りのお好みなどをご記入ください"></textarea>
        </label>
        <fieldset class="reservation-checklist">
          <legend>予約前チェックリスト</legend>
          <ul>
            {checklist.map((item) => <li>{item}</li>)}
          </ul>
        </fieldset>
        <button class="button" type="submit">内容を送信する</button>
      </form>
    </div>
  </section>

  <section class="shell section-gap contact" id="contact">
    <header class="section-heading">
      <span>Support</span>
      <h2>運用サポートと多言語対応で、安心の継続運用。</h2>
    </header>
    <div class="grid contact-grid">
      <article class="card contact-card">
        <h3>多言語サポート</h3>
        <p>日本語 / 英語 / タイ語の3言語でガイドを用意。改行制御と可変フォントで、美しいタイポグラフィを維持します。</p>
      </article>
      <article class="card contact-card">
        <h3>ウォレット決済</h3>
        <p>スマホからのウォレットアプリ決済・ギフトコード入力に対応。Cloudflare PagesとFunctionsで安全に決済連携。</p>
      </article>
      <article class="card contact-card">
        <h3>レビュー運用</h3>
        <p>SNS連携、レビュー返信テンプレート、キャンペーン更新ガイドを整備。継続的な予約流入を支えます。</p>
      </article>
    </div>
  </section>

  <section class="shell section-gap owner" id="owner">
    <header class="section-heading">
      <span>Owner's Voice</span>
      <h2>香りや静けさまで伝わる構成に驚きました。</h2>
    </header>
    <article class="card owner-card">
      <p>
        「店舗の香りや静けさまで伝わる構成に驚きました。スマホからの予約が一気に増え、リピーターの方も
        『サイトが見やすくなって安心した』と好評です。」
      </p>
      <p class="owner-name">Siam Healing オーナー 藤原 様</p>
    </article>
  </section>
</BaseLayout>

<style>
  .section-gap {
    padding-block: 4.5rem;
  }

  .hero {
    display: grid;
    gap: 3rem;
    padding-top: 5rem;
  }

  .hero-pattern {
    position: absolute;
    inset: 0;
    background-image: radial-gradient(circle at 20% 20%, rgba(214, 199, 161, 0.18), transparent 55%),
      radial-gradient(circle at 80% 10%, rgba(202, 169, 105, 0.08), transparent 60%),
      linear-gradient(135deg, rgba(6, 78, 59, 0.15), rgba(11, 91, 68, 0));
    opacity: 0.9;
    z-index: -1;
  }

  .hero-content {
    display: grid;
    gap: 1.2rem;
    position: relative;
    z-index: 1;
  }

  .hero h1 {
    margin: 0;
    font-size: clamp(2.15rem, 9vw, 3.6rem);
    line-height: 1.12;
    letter-spacing: 0.02em;
  }

  .hero-sub {
    margin: 0;
    font-size: 1.02rem;
    max-width: 38ch;
  }

  .hero-actions {
    display: flex;
    flex-wrap: wrap;
    gap: 1rem;
  }

  .hero-metrics {
    display: grid;
    gap: 1rem;
  }

  .metric-card {
    padding: 1.4rem;
  }

  .metric-value {
    margin: 0;
    font-size: 2rem;
    font-weight: 600;
    color: var(--color-gold);
  }

  .metric-label {
    margin: 0.2rem 0 0;
    font-family: var(--font-sans);
    letter-spacing: 0.12em;
    font-size: 0.8rem;
    text-transform: uppercase;
  }

  .metric-description {
    margin: 0.8rem 0 0;
    font-size: 0.92rem;
    color: rgba(255, 255, 255, 0.78);
  }

  .hero-visual {
    display: grid;
    gap: 1.5rem;
  }

  .visual-frame {
    position: relative;
    min-height: 260px;
    border-radius: 24px;
    overflow: hidden;
    border: 1px solid rgba(214, 199, 161, 0.18);
    background: linear-gradient(135deg, rgba(6, 78, 59, 0.9), rgba(15, 118, 110, 0.55));
  }

  .visual-gradient {
    position: absolute;
    inset: 0;
    background-image: linear-gradient(180deg, rgba(214, 199, 161, 0.15), rgba(12, 51, 41, 0.9)),
      url('https://images.unsplash.com/photo-1522335789203-aabd1fc54bc9?auto=format&fit=crop&w=900&q=80');
    background-size: cover;
    background-position: center;
    filter: saturate(0.72);
    mix-blend-mode: screen;
  }

  .visual-caption {
    position: absolute;
    bottom: 1.4rem;
    left: 1.4rem;
    padding: 0.55rem 1rem;
    border-radius: 999px;
    background: rgba(16, 35, 27, 0.7);
    border: 1px solid rgba(214, 199, 161, 0.42);
    font-family: var(--font-sans);
    font-size: 0.78rem;
    letter-spacing: 0.12em;
    text-transform: uppercase;
  }

  .visual-thumbs {
    display: grid;
    gap: 1rem;
  }

  .visual-thumbs div {
    padding: 1rem 1.2rem;
    border-left: 2px solid rgba(214, 199, 161, 0.35);
  }

  .before-after h3 {
    margin-top: 0;
    font-size: 1.2rem;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--color-sand);
  }

  .before-after ul {
    padding-left: 1rem;
    margin: 0.8rem 0 0;
    display: grid;
    gap: 0.65rem;
  }

  .concept {
    position: relative;
  }

  .concept-grid {
    display: grid;
    gap: 1.5rem;
  }

  .concept-card h3 {
    margin-top: 0;
    font-size: 1.3rem;
  }

  .palette {
    display: flex;
    gap: 0.6rem;
    flex-wrap: wrap;
  }

  .palette span {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 92px;
    height: 92px;
    border-radius: 16px;
    background: var(--swatch);
    font-size: 0.72rem;
    font-family: var(--font-sans);
    letter-spacing: 0.05em;
    box-shadow: inset 0 0 0 1px rgba(16, 35, 27, 0.15);
  }

  .concept-list {
    margin: 0;
    padding-left: 1rem;
    display: grid;
    gap: 0.6rem;
  }

  .interaction-note {
    display: grid;
    gap: 0.9rem;
    padding: 1rem 1.1rem;
    border-radius: 16px;
    background: rgba(7, 47, 37, 0.6);
  }

  .experience-grid {
    gap: 1.2rem;
  }

  .experience-card h3 {
    margin: 0.4rem 0 1rem;
    font-size: 1.35rem;
  }

  .experience-card .card-detail {
    margin-top: 1rem;
    font-size: 0.9rem;
    color: rgba(214, 199, 161, 0.7);
  }

  .dynamic-grid {
    display: grid;
    gap: 1.2rem;
  }

  .dynamic-card[data-time='Morning'] {
    border-left: 3px solid rgba(214, 199, 161, 0.35);
  }
  .dynamic-card[data-time='Day'] {
    border-left: 3px solid rgba(15, 118, 110, 0.55);
  }
  .dynamic-card[data-time='Night'] {
    border-left: 3px solid rgba(202, 169, 105, 0.65);
  }

  .dynamic-time {
    font-family: var(--font-sans);
    letter-spacing: 0.3em;
    text-transform: uppercase;
    color: rgba(214, 199, 161, 0.7);
  }

  .dynamic-card h3 {
    margin: 0.6rem 0 0.4rem;
    font-size: 1.4rem;
  }

  .dynamic-accent {
    display: inline-flex;
    margin-top: 1rem;
    padding: 0.45rem 0.9rem;
    border-radius: 999px;
    background: rgba(214, 199, 161, 0.12);
    border: 1px solid rgba(214, 199, 161, 0.2);
    font-family: var(--font-sans);
    font-size: 0.78rem;
    letter-spacing: 0.08em;
  }

  .course-grid {
    gap: 1.2rem;
  }

  .course-card h3 {
    margin: 0;
    font-size: 1.4rem;
  }

  .course-meta {
    margin: 0.4rem 0 0;
    font-family: var(--font-sans);
    font-size: 0.9rem;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: rgba(214, 199, 161, 0.8);
  }

  .course-ideal {
    margin: 0.6rem 0 1rem;
    font-size: 0.95rem;
    color: rgba(214, 199, 161, 0.7);
  }

  .course-card ul {
    margin: 0;
    padding-left: 1rem;
    display: grid;
    gap: 0.55rem;
  }

  .course-result {
    margin: 1rem 0 1.5rem;
    font-size: 0.95rem;
    color: rgba(255, 255, 255, 0.82);
  }

  .results-grid {
    gap: 1.2rem;
  }

  .result-card h3 {
    margin-top: 0;
    font-size: 1.25rem;
  }

  .voice-grid {
    gap: 1.2rem;
  }

  .voice-card {
    background: rgba(16, 35, 27, 0.72);
    border-radius: 28px;
    padding: 2rem 1.6rem;
    position: relative;
  }

  .voice-card::before {
    content: '';
    position: absolute;
    inset: 18px;
    border-radius: 24px;
    border: 1px dashed rgba(214, 199, 161, 0.2);
    pointer-events: none;
  }

  .voice-message {
    margin: 0;
    font-size: 1rem;
  }

  .voice-name {
    margin: 1.2rem 0 0;
    font-family: var(--font-sans);
    letter-spacing: 0.14em;
    text-transform: uppercase;
  }

  .voice-role {
    margin: 0.3rem 0 0;
    font-size: 0.85rem;
    color: rgba(214, 199, 161, 0.65);
  }

  .reservation-wrapper {
    display: grid;
    gap: 2rem;
  }

  .reservation-progress {
    position: relative;
    padding: 1.5rem 1.8rem;
    border-radius: 20px;
    background: rgba(16, 35, 27, 0.6);
    border: 1px solid rgba(214, 199, 161, 0.12);
  }

  .reservation-progress ol {
    margin: 0;
    padding: 0;
    list-style: none;
    display: grid;
    gap: 1.5rem;
  }

  .reservation-progress li span {
    display: block;
    font-family: var(--font-sans);
    font-size: 0.75rem;
    letter-spacing: 0.28em;
    text-transform: uppercase;
    color: rgba(214, 199, 161, 0.7);
  }

  .reservation-progress li strong {
    display: block;
    margin-top: 0.4rem;
    font-size: 1.15rem;
  }

  .reservation-form {
    display: grid;
    gap: 1.1rem;
  }

  .reservation-form label,
  .reservation-form fieldset {
    display: grid;
    gap: 0.5rem;
    font-size: 0.92rem;
  }

  .reservation-form input,
  .reservation-form select,
  .reservation-form textarea {
    padding: 0.75rem 0.9rem;
    border-radius: 12px;
    border: 1px solid rgba(214, 199, 161, 0.25);
    background: rgba(7, 47, 37, 0.65);
    color: var(--color-mist);
    font-family: var(--font-sans);
    font-size: 0.95rem;
  }

  .reservation-form input:focus-visible,
  .reservation-form select:focus-visible,
  .reservation-form textarea:focus-visible {
    outline: 2px solid rgba(202, 169, 105, 0.6);
    outline-offset: 2px;
  }

  .cta-switcher {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 0.5rem;
  }

  .cta-switcher input {
    position: absolute;
    opacity: 0;
    pointer-events: none;
  }

  .cta-switcher label {
    text-align: center;
    padding: 0.65rem 0.5rem;
    border-radius: 12px;
    background: rgba(214, 199, 161, 0.12);
    border: 1px solid transparent;
    font-family: var(--font-sans);
    font-size: 0.83rem;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    transition: var(--transition-slow);
  }

  .cta-switcher input:checked + label {
    background: rgba(202, 169, 105, 0.18);
    border-color: rgba(202, 169, 105, 0.4);
  }

  .reservation-intro {
    margin: 0.3rem 0 0.2rem;
    font-size: 0.92rem;
    color: rgba(255, 255, 255, 0.75);
  }

  .reservation-checklist {
    border-radius: 14px;
    border: 1px solid rgba(214, 199, 161, 0.2);
    background: rgba(7, 47, 37, 0.5);
    padding: 1rem 1.2rem;
  }

  .reservation-checklist legend {
    font-family: var(--font-sans);
    font-size: 0.82rem;
    letter-spacing: 0.2em;
    text-transform: uppercase;
  }

  .reservation-checklist ul {
    margin: 0.5rem 0 0;
    padding-left: 1.2rem;
    display: grid;
    gap: 0.5rem;
    font-size: 0.9rem;
  }

  .contact-grid,
  .owner-card {
    gap: 1.2rem;
  }

  .contact-card h3 {
    margin-top: 0;
    font-size: 1.3rem;
  }

  .owner-card {
    display: grid;
    gap: 1rem;
    font-size: 1rem;
  }

  .owner-name {
    margin: 0;
    font-family: var(--font-sans);
    letter-spacing: 0.16em;
    text-transform: uppercase;
    color: rgba(214, 199, 161, 0.8);
  }

  @media (min-width: 768px) {
    .hero {
      grid-template-columns: 1.3fr 1fr;
      align-items: center;
    }

    .hero-metrics {
      grid-template-columns: repeat(3, minmax(0, 1fr));
    }

    .visual-frame {
      min-height: 340px;
    }

    .visual-thumbs {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }

    .concept-grid {
      grid-template-columns: repeat(3, minmax(0, 1fr));
    }

    .experience-grid {
      grid-template-columns: repeat(4, minmax(0, 1fr));
    }

    .dynamic-grid {
      grid-template-columns: repeat(3, minmax(0, 1fr));
    }

    .course-grid {
      grid-template-columns: repeat(3, minmax(0, 1fr));
    }

    .results-grid {
      grid-template-columns: repeat(3, minmax(0, 1fr));
    }

    .voice-grid {
      grid-template-columns: repeat(3, minmax(0, 1fr));
    }

    .reservation-wrapper {
      grid-template-columns: minmax(0, 0.9fr) minmax(0, 1.1fr);
    }

    .contact-grid {
      grid-template-columns: repeat(3, minmax(0, 1fr));
    }
  }

  @media (min-width: 1024px) {
    .section-gap {
      padding-block: 6rem;
    }

    .hero {
      padding-top: 7rem;
    }

    .hero-content {
      gap: 1.5rem;
    }

    .hero h1 {
      font-size: clamp(3.2rem, 5vw, 4rem);
    }

    .visual-frame {
      min-height: 420px;
    }

    .metric-card {
      padding: 1.6rem;
    }
  }
</style>
